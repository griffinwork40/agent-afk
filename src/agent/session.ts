/**
 * Agent session wrapper (barrel).
 *
 * The implementation lives in `src/agent/session/*` to keep files small while
 * preserving the public `AgentSession` API.
 *
 * @module agent/session
 */

export { AgentSession } from './session/agent-session.js';

