---
name: Bug report
about: Report a problem with Agent AFK
title: ''
labels: bug
assignees: ''
---

**What happened**
A clear description of the bug.

**Expected behavior**
What you expected instead.

**Repro steps**
1. Command run (e.g. `afk chat "..."`, `afk i`, `afk daemon`)
2. ...

**Environment**
- `agent-afk` version (`afk --version`):
- Node version (`node --version`):
- OS:
- Model / provider (e.g. `sonnet`, `gpt-...`):

**`afk doctor` output**
```
paste the output of `afk doctor` here (redact any secrets)
```

**Logs / trace**
Any relevant output. **Redact API keys and tokens.**
